[package]
name = "rust_example_plugin"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
wit-bindgen = { version = "0.16.0", features = ["realloc"] }

[profile.release]
opt-level = "s"
lto = true