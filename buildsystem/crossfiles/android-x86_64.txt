# Cross-compilation configuration for Android x86_64
# Based on VideoLAN's vlc-android build system
# Requires ANDROID_NDK_HOME environment variable

[binaries]
c = 'clang'
cpp = 'clang++'
ar = 'llvm-ar'
strip = 'llvm-strip'

[host_machine]
system = 'android'
cpu_family = 'x86_64'
cpu = 'x86_64'
endian = 'little'

[properties]
c_args = ['-target', 'x86_64-linux-android21', '-fPIC', '-DANDROID']
c_link_args = ['-target', 'x86_64-linux-android21']
android_ndk = '@0@'.format(getenv('ANDROID_NDK_HOME'))