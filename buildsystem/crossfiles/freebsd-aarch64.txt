# Cross-compilation configuration for FreeBSD ARM64
# Requires FreeBSD toolchain or cross build environment

[binaries]
c = 'clang'
cpp = 'clang++'
ar = 'ar'
strip = 'strip'

[host_machine]
system = 'freebsd'
cpu_family = 'aarch64'
cpu = 'aarch64'
endian = 'little'

[properties]
c_args = ['-target', 'aarch64-unknown-freebsd', '-fPIC']
c_link_args = ['-target', 'aarch64-unknown-freebsd']
sys_root = '/usr/local/freebsd-sysroot-aarch64'